[project]
name = "carleton-courses-mcp"
version = "0.1.0"
description = "MCP server to serve Carleton course data"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
    "pypdf>=4.3.1",
]
